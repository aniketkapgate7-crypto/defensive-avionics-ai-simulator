# Models

The subdirectories hold local checkpoints for each module. Weight files are
ignored by Git. Store a small metadata JSON beside every final exported model
containing its configuration, seed, dataset version, metrics, and creation date.
